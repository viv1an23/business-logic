{% extends 'base.html.twig' %}
{% set active = 'team' %}
{% block title %} Teams {% endblock %}
{% block content %}
    <a href="{{ path('app_team_action') }}" class="mb-3 btn btn-primary" data-bs-popup="tooltip" data-bs-original-title="Add Team">Add Team</a>
    <div class="card">
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>{{ knp_pagination_sortable(teams, 'Id', 'id') }}</th>
                    <th>{{ knp_pagination_sortable(teams, 'Name', 'name') }}</th>
                    <th>{{ knp_pagination_sortable(teams, 'Country', 'country') }}</th>
                    <th>{{ knp_pagination_sortable(teams, 'Money Balance', 'moneyBalance') }}</th>
                    <th class="table-actions" colspan="5">Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for team in teams %}
                    {% include 'teams/row.html.twig' %}
                {% else %}
                    <tr class="w-100">
                        <td class="border-none" colspan="8">No Records found</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
        {{ include('paginator/pagination_card_footer.html.twig', {'knp_result_set': teams}) }}
    </div>
{% endblock %}

{#{% block javascripts %}#}
{#    <script>#}
{#        var delete_game_url = "{{path("cp_game_delete", {'id': "__game_id__"})}}";#}
{#    </script>#}
{#    <script src="{{ asset('assets/js/delete_game.js') }}"></script>#}
{#{% endblock  %}#}
